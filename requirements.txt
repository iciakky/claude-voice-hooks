# Claude Voice Hooks - Python Dependencies

# Audio playback (cross-platform, stable)
playsound3>=2.0.0

# FastAPI server (Phase 1)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# HTTP client (for testing)
httpx>=0.25.0

# System requirements:
# - Python 3.8+
# - Ollama with gemma3n:e4b model (for intent classification)
